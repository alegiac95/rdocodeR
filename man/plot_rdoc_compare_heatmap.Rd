% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rdoc_heatmap_compare.R
\name{plot_rdoc_compare_heatmap}
\alias{plot_rdoc_compare_heatmap}
\title{Plot RDoC Comparison Heatmap Across Conditions}
\usage{
plot_rdoc_compare_heatmap(
  corr_list,
  term_alignment = c("union", "strict", "intersection"),
  domain_palette = "Accent",
  significance_level = 0.05,
  show_significance_stars = TRUE,
  domain_gap = 0.55,
  annotation_height = 0.48,
  annotation_to_heatmap_gap = 1.2,
  compact_small_domain_labels = FALSE,
  small_domain_threshold = 5,
  domain_label_wrap_width = 22,
  show_term_labels = FALSE,
  show_significant_term_labels = FALSE,
  term_label_wrap_width = 18,
  significant_term_label_angle = 45,
  na_fill = "grey90",
  correlation_label = c("pearson", "spearman"),
  output_file = NULL,
  width = 12,
  height = 5.5,
  dpi = 300
)
}
\arguments{
\item{corr_list}{List of correlation data frames, each with columns \code{Domain},
\code{Term}, \code{r}, and optional \code{p}.}

\item{term_alignment}{How terms are aligned across conditions:
\code{"union"} (default) keeps all terms, \code{"intersection"} keeps only shared terms,
and \code{"strict"} requires identical term sets.}

\item{domain_palette}{Palette name from \code{\link[=rdoc_available_palettes]{rdoc_available_palettes()}}.}

\item{significance_level}{Threshold used for significance stars (\code{p < threshold}).}

\item{show_significance_stars}{Logical; draw \code{*} on significant cells.}

\item{domain_gap}{Horizontal gap inserted between domains.}

\item{annotation_height}{Height of the domain/term annotation rows.}

\item{annotation_to_heatmap_gap}{Vertical gap between annotation rows and
the first condition row.}

\item{compact_small_domain_labels}{Logical; when \code{TRUE}, domains with fewer
than \code{small_domain_threshold} terms use short labels (\code{AR}, \code{NV}, \code{SP}, etc.).}

\item{small_domain_threshold}{Integer threshold used with
\code{compact_small_domain_labels}.}

\item{domain_label_wrap_width}{Wrap width for domain labels.}

\item{show_term_labels}{Logical; show term labels on the bottom axis.}

\item{show_significant_term_labels}{Logical; show only terms with at least one
significant result (\code{p < significance_level}) on the bottom x-axis.
Ignored when \code{show_term_labels = TRUE}.}

\item{term_label_wrap_width}{Target wrap width if \code{show_term_labels = TRUE}.}

\item{significant_term_label_angle}{Angle (in degrees) used when
\code{show_significant_term_labels = TRUE}.}

\item{na_fill}{Fill color for missing term-condition cells (for example when
\code{term_alignment = "union"} and a term is absent in one condition).}

\item{correlation_label}{Label style used for the correlation legend:
\code{"pearson"} (default, \verb{Pearson r}) or \code{"spearman"} (\verb{Spearman rho}).}

\item{output_file}{Optional output path. If provided, plot is saved with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height}{Figure size in inches when \code{output_file} is provided.}

\item{dpi}{Resolution when \code{output_file} is provided.}
}
\value{
A ggplot object.
}
\description{
Builds a term-by-condition heatmap from multiple RDoC correlation tables,
with two annotation rows that match the circular plot colors:
a domain-color row and a term-color row.
}
\examples{
df1 <- rdoc_example_data()
df2 <- df1
df3 <- df1
set.seed(1)
df2$r <- pmax(pmin(df2$r + stats::rnorm(nrow(df2), sd = 0.1), 1), -1)
df3$r <- pmax(pmin(df3$r + stats::rnorm(nrow(df3), sd = 0.15), 1), -1)
p <- plot_rdoc_compare_heatmap(list(Sample_A = df1, Sample_B = df2, Sample_C = df3))
p
}
