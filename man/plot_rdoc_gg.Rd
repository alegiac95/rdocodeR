% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_rdoc_gg.R
\name{plot_rdoc_gg}
\alias{plot_rdoc_gg}
\title{Plot Circular RDoC Correlations with ggplot2}
\usage{
plot_rdoc_gg(
  corr_df,
  domain_palette = "Accent",
  output_file = NULL,
  width = 10,
  height = 10,
  dpi = 300,
  angular_gap_term = 0.1,
  angular_gap_domain = 0.1,
  radial_gap_domains = 0.05,
  significance_level = 0.05,
  show_term_labels = TRUE,
  expand_domain_labels = FALSE,
  highlight_significant_terms = FALSE,
  show_only_significant_term_labels = FALSE,
  show_bar_values = FALSE,
  bar_value_digits = 2,
  bar_value_size = 2.3,
  term_label_offset = 0.16,
  term_label_size = 3.2,
  expand_term_abbreviations = TRUE,
  term_label_wrap_width = 28
)
}
\arguments{
\item{corr_df}{Data frame with columns \code{Domain}, \code{Term}, \code{r}, and optional \code{p}.
\code{Term} must be unique.}

\item{domain_palette}{Palette name from \code{\link[=rdoc_available_palettes]{rdoc_available_palettes()}}.}

\item{output_file}{Optional output path. If provided, plot is saved with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height}{Figure size in inches when \code{output_file} is provided.}

\item{dpi}{Resolution when \code{output_file} is provided.}

\item{angular_gap_term}{Angular gap between adjacent terms in the middle ring.}

\item{angular_gap_domain}{Angular gap between adjacent domains in the outer ring.}

\item{radial_gap_domains}{Radial gap between middle and outer rings.}

\item{significance_level}{Threshold used to draw significance stars (\code{p < threshold}).}

\item{show_term_labels}{Logical; draw term labels outside the domain ribbon.}

\item{expand_domain_labels}{Logical; when \code{TRUE}, domain codes are expanded
in the outer ring labels (for example \code{CS} to \verb{Cognitive Systems}).}

\item{highlight_significant_terms}{Logical; when \code{TRUE}, term labels with
\code{p < significance_level} are drawn in bold.}

\item{show_only_significant_term_labels}{Logical; when \code{TRUE}, only labels
for terms with \code{p < significance_level} are drawn.}

\item{show_bar_values}{Logical; when \code{TRUE}, show correlation values for
significant bars (\code{p < significance_level}) inside the bars.}

\item{bar_value_digits}{Number of decimals used for bar value labels.}

\item{bar_value_size}{Text size for bar value labels.}

\item{term_label_offset}{Radial offset for external term labels.}

\item{term_label_size}{Text size for external term labels.}

\item{expand_term_abbreviations}{Logical; expand abbreviations in term labels:
\code{RL} to Reinforcement Learning, \code{CC} to Cognitive Control,
and \code{WM} to Working Memory.}

\item{term_label_wrap_width}{Target width for two-line label wrapping.}
}
\value{
A ggplot object.
}
\description{
Creates a circular RDoC plot using ggplot2 with three bands:
outer domains, middle constructs, and inner correlation bars.
}
\examples{
df <- rdoc_example_data()
p <- plot_rdoc_gg(df, domain_palette = "Accent")
p
}
